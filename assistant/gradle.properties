org.gradle.jvmargs=-Xmx2048m
# Enabled because the Google Play Billing library depends on AndroidX.
# (Our own code uses only the framework, so this is harmless.)
android.useAndroidX=true
android.nonTransitiveRClass=true
